import React from 'react'
import { View } from 'react-native'

import styles from '../../Styles/components/Tabs.style'

const Tabs: React.FC = () => {
  return <View style={styles.container} />
}

export default Tabs
