import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({

  container: {
    alignItems: 'center',
    paddingTop: 40,
    paddingRight: 0,
    paddingLeft: 0,
    paddingBottom: 30
  },

  top: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 10
  },

  logo: {

  },

  title: {
    fontSize: 18,
    color: '#fff',
    fontWeight: 'bold',
    marginLeft: 8
  }

})

export default styles
